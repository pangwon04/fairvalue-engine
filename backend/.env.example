# FairValue Backend — DB 접속 변수 템플릿
# 실제 값은 .env 로 복사해 채운다(.env 는 커밋하지 않는다).

# JDBC URL (host:port/db)
DB_URL=jdbc:postgresql://localhost:5432/fairvalue
DB_USER=fairvalue
DB_PASSWORD=fairvalue

# 선택: 활성 프로필 / 포트
SPRING_PROFILES_ACTIVE=local
SERVER_PORT=8080
