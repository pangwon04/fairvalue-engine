-- ===========================================================================
-- V6 — 평가시점 입력 스냅샷(context_json) 저장. 5-7 계산근거 '입력 스냅샷' 원천.
--   additive: 신규 컬럼만. 기존 결과·계약 불변. 구 job 은 NULL(프론트 폴백 안내).
-- ===========================================================================
ALTER TABLE pricing_jobs
    ADD COLUMN context_json JSONB;
