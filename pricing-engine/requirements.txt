# FairValue Pricing Engine 런타임 의존성
# 계산기는 pure Python(math)이라 numpy 불필요. pydantic 은 result.py 검증.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.5,<3.0
# 테스트
pytest>=8.0
httpx>=0.27          # FastAPI TestClient 용
